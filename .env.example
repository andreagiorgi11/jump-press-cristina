# Two Vercel projects, same repository. press is the safe default.
JUMP_SITE=press
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
JUMP_PUBLIC_URL=
# Optional operational alert receiver; accepts JSON without personal data.
JUMP_ALERT_WEBHOOK_URL=
# Only on the News deployment, if already in use:
X_BEARER_TOKEN=
