 'use client';
import {useState} from 'react';
export default function DownloadTest(){const [busy,setBusy]=useState(false),[result,setResult]=useState(null);
async function run(){setBusy(true);setResult(null);try{const r=await fetch('/api/editor/download-test',{method:'POST'});setResult(await r.json());}catch{setResult({error:'Risposta non ricevuta. Verificare i log prima di ripetere.'});}finally{setBusy(false);}}
return <main className="editor-main narrow"><h1>Prova preparazione Ecostampa</h1><p>PDF del 17 settembre: download, estrazione dei testi e immagine della pagina 10. Nessuna modifica a bozze o pubblicazioni. Il file temporaneo viene eliminato al termine.</p><button disabled={busy} onClick={run}>{busy?'Preparazione in corso…':'Avvia prova sul server'}</button>{result&&<pre style={{whiteSpace:'pre-wrap',overflowWrap:'anywhere'}}>{JSON.stringify({...result,preview:result.preview?'Immagine della pagina 10 disponibile sotto':undefined},null,2)}</pre>}{result?.preview&&<img src={result.preview} alt="Pagina 10 del PDF originale, resa dal server" style={{width:'100%',height:'auto'}}/>}</main>;}
